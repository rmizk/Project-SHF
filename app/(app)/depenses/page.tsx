import PagePlaceholder from "@/components/PagePlaceholder";

export default function DepensesPage() {
  return (
    <PagePlaceholder
      title="Dépenses"
      description="La gestion des dépenses (avec facture, sans facture, particulier) sera implémentée dans une prochaine étape."
    />
  );
}
